version: 0.0
os: linux
files:
  - source: /
    destination: /home/webapp/qanda

permissions:
  - object: /home/webapp/qanda
    owner: webapp
    group: webapp

hooks:
  AfterInstall:
    - location: ./scripts/after_install.sh
      timeout: 60
      runas: webapp